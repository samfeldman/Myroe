<!DOCTYPE html>
<html>
<head>
  <title>Myroe</title>
  <link href='http://fonts.googleapis.com/css?family=Muli:400,400italic' rel='stylesheet' type='text/css'>
  <script src="https://maps.googleapis.com/maps/api/js?v=3.exp"></script>
  <%= stylesheet_link_tag    'application', media: 'all'%>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
	<main class="clearfix">
		<div id="mapdiv">
		</div>
		<section class="over_map">
			<% if @current_user %>
				<nav class="top_nav clearfix">
					<ul class="clearfix">
						<li class="bucket_nav"><a href="/users/<%= @current_user.id %>">My Bucket</a></li>
						<li class="settings_nav">Settings</li>
						<li><a href="/sessions/destroy">Logout</a></li>
					</ul>
					<div class="settings_drop content">
						<div class="settings_close">CLOSE</div>
						<h1>Settings</h1>
						<%= form_for @current_user do |f| %>
							<%= f.text_field :username %>
							<%= f.text_field :zipcode, :placeholder => "zipcode" %>
							<%= f.email_field :email %>
							<%= f.password_field :password, :placeholder => "type password here" %>
							<%= f.password_field :password_confirmation, :placeholder => "confirm password" %>
							<%= f.submit "update" %>
							<button>
								<%= link_to ("delete user"), "/users/#{ @current_user.id }", method: :delete %>
							</button>
						<% end %>
					</div>
				</nav>
			<% end %>
			<%= flash[:alert] if flash[:alert] %>
			<%= flash[:notice] if flash[:notice] %>
			<%= yield %>
		</section>
	</main>
</body>
</html>
